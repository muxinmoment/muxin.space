<section class="wander-3d-shell" data-wander-3d>
  <div class="wander-3d-copy">
    <p class="eyebrow">Pixel wander room · interactive space</p>
    <h2>欢迎来到会变化的像素工作室。</h2>
    <p>这里没有导航栏。拖动镜头看看，靠近一件物品，选择你想先发现的那条支线。</p>
  </div>
  <div class="wander-3d-stage">
    <canvas data-wander-canvas aria-label="漫游房间 3D 场景"></canvas>
    <div class="wander-3d-hint" data-room-hint>拖动看看 · 点击物件开始漫游</div>
    <div class="wander-3d-memory" aria-live="polite"><span data-room-memory></span><span data-room-progress></span></div>
    <div class="wander-3d-fallback" data-wander-fallback>
      <span>3D 场景加载失败</span>
      <a href="/wander/" class="underline">继续使用房间入口</a>
    </div>
    <div class="wander-3d-controls" aria-label="房间入口">
      <button type="button" data-room-camera="center" aria-pressed="true">回到房间</button>
      <button type="button" data-room-camera="anime" aria-pressed="false">番剧书架</button>
      <button type="button" data-room-camera="photo" aria-pressed="false">照片墙</button>
      <button type="button" data-room-camera="notes" aria-pressed="false">支线随笔</button>
      <button type="button" data-room-camera="memo" aria-pressed="false">小木电台</button>
    </div>
    <a href="/wander/" class="wander-3d-enter" data-room-enter>逛一圈 →</a>
    <button type="button" class="wander-3d-skip" data-wander-3d-skip>使用文字目录</button>
  </div>
  <div class="wander-game-panel" data-wander-game-panel hidden aria-live="polite">
    <div class="wander-game-heading">
      <div>
        <p class="wander-game-kicker">MINIGAME 01 · ANIME SHELF</p>
        <h3>整理书架，找回这一格记忆。</h3>
      </div>
      <button type="button" class="wander-game-close" data-wander-game-close aria-label="关闭小游戏">×</button>
    </div>
    <p class="wander-game-copy">选一本书，再选一个位置。把三本书放回正确顺序，不限时间。</p>
    <div class="wander-book-slots" data-wander-book-slots aria-label="书架位置"></div>
    <div class="wander-book-tray" data-wander-book-tray aria-label="待整理的书"></div>
    <p class="wander-game-status" data-wander-game-status>先选一本书。</p>
    <div class="wander-game-actions">
      <button type="button" data-wander-game-reset>重新打乱</button>
      <a href="/wander/anime/" data-wander-game-skip>跳过，直接进入书架 →</a>
    </div>
    <div class="wander-game-complete" data-wander-game-complete hidden>
      <strong>书架亮起来了。</strong>
      <span>你找到了一条番剧支线，继续往里面看看。</span>
      <a href="/wander/anime/">进入番剧书架 →</a>
    </div>
  </div>
</section>

<style>
  .wander-3d-shell { margin-top: 2rem; overflow: hidden; border: 3px solid #332449; border-radius: 1.25rem; background: #1c1830; color: #fff7ed; box-shadow: 0 1.25rem 0 #0f0d1a, 0 2rem 3rem rgba(31, 21, 55, .22); }
  .wander-3d-copy { padding: 1.5rem 1.5rem 0; }
  .wander-3d-copy h2 { margin-top: .5rem; font-family: var(--font-serif); font-size: 1.8rem; letter-spacing: -.03em; }
  .wander-3d-copy p:last-child { max-width: 38rem; margin-top: .65rem; color: #b9adcf; font-size: .875rem; line-height: 1.75rem; }
  .wander-3d-stage { position: relative; min-height: 500px; }
  .wander-3d-stage canvas { display: block; width: 100%; height: 500px; cursor: grab; touch-action: none; image-rendering: pixelated; image-rendering: crisp-edges; }
  .wander-3d-stage canvas:active { cursor: grabbing; }
  .wander-3d-controls { position: absolute; left: 1rem; bottom: 1rem; display: flex; flex-wrap: wrap; gap: .45rem; }
  .wander-3d-controls button, .wander-3d-skip { border: 2px solid #5b4378; border-radius: .45rem; background: #211a35; padding: .45rem .75rem; color: #f4d8ff; font-size: .72rem; box-shadow: 2px 2px 0 #0f0d1a; transition: background .2s, border-color .2s, transform .2s; }
  .wander-3d-controls button:hover, .wander-3d-controls button[data-active="true"] { border-color: #ffd166; background: #704c9e; color: white; transform: translate(-1px, -1px); }
  .wander-3d-enter { position: absolute; right: 6.5rem; bottom: 1rem; border: 2px solid #fff0c2; border-radius: .45rem; background: #ffd166; padding: .45rem .75rem; color: #38254f; font-size: .72rem; font-weight: 700; box-shadow: 2px 2px 0 #0f0d1a; transition: transform .2s, background .2s; }
  .wander-3d-enter:hover { transform: translate(-1px, -1px); background: #ffe29a; }
  .wander-3d-skip { position: absolute; right: 1rem; bottom: 1rem; color: #a8a29e; }
  .wander-3d-hint { position: absolute; top: 1rem; left: 1rem; border: 2px solid #5b4378; border-radius: .45rem; background: #211a35; padding: .4rem .7rem; color: #f4d8ff; font-size: .68rem; box-shadow: 2px 2px 0 #0f0d1a; transition: opacity .2s; }
  .wander-3d-memory { position: absolute; top: 1rem; right: 1rem; display: grid; gap: .15rem; max-width: 13rem; border: 2px solid #5b4378; border-radius: .45rem; background: #211a35; padding: .45rem .7rem; color: #f0dff7; font-size: .68rem; text-align: right; box-shadow: 2px 2px 0 #0f0d1a; }
  .wander-3d-memory span:empty { display: none; }
  .wander-3d-memory span:last-child { color: rgba(196,181,253,.8); }
  .wander-3d-fallback { position: absolute; inset: 0; display: none; place-items: center; gap: .75rem; color: #c4b5fd; font-size: .875rem; }
  .wander-game-panel { border-top: 3px solid #332449; background: #211a35; padding: 1.25rem 1.5rem 1.5rem; color: #fff7ed; }
  .wander-game-heading { display: flex; align-items: flex-start; justify-content: space-between; gap: 1rem; }
  .wander-game-kicker { color: #ffd166; font-size: .65rem; letter-spacing: .16em; }
  .wander-game-heading h3 { margin-top: .35rem; font-family: var(--font-serif); font-size: 1.35rem; }
  .wander-game-close { border: 2px solid #5b4378; border-radius: .4rem; padding: .15rem .55rem; color: #f4d8ff; font-size: 1.25rem; line-height: 1; }
  .wander-game-copy { margin-top: .5rem; color: #d9c8e5; font-size: .8rem; line-height: 1.5rem; }
  .wander-book-slots, .wander-book-tray { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: .6rem; }
  .wander-book-slots { margin-top: 1rem; }
  .wander-book-tray { margin-top: .75rem; min-height: 3.5rem; }
  .wander-book-slot, .wander-book { min-height: 3.5rem; border: 2px solid #76568a; border-radius: .35rem; padding: .55rem .45rem; font-size: .75rem; box-shadow: 3px 3px 0 #0f0d1a; transition: transform .15s, border-color .15s, background .15s; }
  .wander-book-slot { background: #302146; color: #cdb8dc; }
  .wander-book-slot[data-selected="true"], .wander-book[data-selected="true"] { border-color: #ffd166; transform: translate(-1px, -1px); }
  .wander-book { background: #704c9e; color: #fff7ed; cursor: pointer; }
  .wander-book:hover, .wander-book-slot:hover { border-color: #ffd166; transform: translate(-1px, -1px); }
  .wander-game-status { min-height: 1.25rem; margin-top: .8rem; color: #e7c6ff; font-size: .75rem; }
  .wander-game-actions { display: flex; flex-wrap: wrap; align-items: center; gap: .75rem; margin-top: .5rem; font-size: .72rem; }
  .wander-game-actions button, .wander-game-actions a, .wander-game-complete a { border: 2px solid #5b4378; border-radius: .35rem; padding: .4rem .65rem; color: #f4d8ff; }
  .wander-game-actions button:hover, .wander-game-actions a:hover, .wander-game-complete a:hover { border-color: #ffd166; color: #fff7ed; }
  .wander-game-complete { display: grid; gap: .3rem; margin-top: 1rem; border: 2px solid #d59b37; border-radius: .4rem; background: #4d3a30; padding: .8rem; color: #fff1bd; font-size: .78rem; }
  .wander-game-complete span { color: #f7dfac; }
  .wander-game-complete a { width: fit-content; margin-top: .3rem; border-color: #d59b37; color: #fff1bd; }
  .wander-3d-shell.is-disabled .wander-3d-stage { display: none; }
  .wander-3d-shell.is-disabled .wander-3d-copy { padding-bottom: 1.5rem; }
  .wander-3d-shell.is-failed .wander-3d-fallback { display: grid; }
  @media (max-width: 639px) { .wander-3d-stage, .wander-3d-stage canvas { min-height: 430px; height: 430px; } .wander-3d-controls { right: 1rem; bottom: 3.6rem; } .wander-3d-enter { right: auto; left: 1rem; } }
  @media (max-width: 639px) { .wander-game-panel { padding: 1rem; } .wander-book-slots, .wander-book-tray { gap: .4rem; } .wander-book-slot, .wander-book { min-height: 3.25rem; font-size: .68rem; } }
  @media (prefers-reduced-motion: reduce) { .wander-3d-controls button, .wander-3d-enter, .wander-3d-hint { transition: none; } }
  @media (min-width: 640px) { .wander-3d-copy { padding: 2rem 2rem 0; } }
</style>

<script src="../scripts/wander-room.ts"></script>
<script src="../scripts/wander-book-game.ts"></script>
